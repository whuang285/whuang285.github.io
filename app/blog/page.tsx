import Link from 'next/link'; import Image from 'next/image'; import {getPosts} from '@/lib/posts';
export default function Blog(){const posts=getPosts();const years=[...new Set(posts.map(p=>p.date.slice(0,4)))];return <><section className="page-hero"><div className="container"><div className="eyebrow">Writing</div><h1>Travel notes & guides.</h1><p>The original blog content is preserved and rendered through a new editorial layout. No Jekyll templates required.</p></div></section><section className="section"><div className="container"><div className="archive">{years.map(y=><div className="year-group" key={y}><div className="year">{y}</div><div className="archive-list">{posts.filter(p=>p.date.startsWith(y)).map(p=><Link className="archive-row" href={`/blog/${p.slug}/`} key={p.slug}><span className="date">{p.date.slice(5)}</span><span className="title">{p.title}</span><span className="tag">{p.tags[0]||'notes'}</span></Link>)}</div></div>)}</div></div></section></>}
